# Root outputs. Empty for now — Phase 1 adds VPC outputs, Phase 2 adds cluster
# outputs, Phases 3-4 add Karpenter outputs. See interface-contract.md SS4 for the
# full, final list this file must converge on.
